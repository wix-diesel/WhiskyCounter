# WhiskyCounter

飲んだウイスキーの量をカウントし、アルコール量を計算するサイト。

## 機能

- 「＋ 試飲」で1杯追加
- 1杯あたりの量を5 / 10 / 15mlまたは任意の値に設定
- ウイスキー度数を下限〜上限で設定（初期値40〜60%）
- 試飲数、推定総量、純アルコール量を表示
- カウントと設定はブラウザのlocalStorageに保存
- PWAとしてiPhoneのホーム画面に追加可能

純アルコール量は `飲酒量(ml) × ABV × 0.8` で概算します。
